import React, { FC } from "react";
import logo from "./logo.svg";
import "./App.css";

interface AppProps {}
const App: FC<AppProps> = () => {
  return <div>flowchart builder !</div>;
};

export default App;
